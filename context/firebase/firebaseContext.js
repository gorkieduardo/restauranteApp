
// Primero importamos en Context de react
import {createContext} from 'react';

// Crebamos la variable correspondiente que va a manejar el context
const FirebaseContext = createContext();

// finalmente, exportamos el componente
export default FirebaseContext;
